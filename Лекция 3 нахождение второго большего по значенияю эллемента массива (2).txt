array = [1, 8, 3, 2, 6]
size = [5]
current_index = [0]
max_current_index = [0]
max = array [0]
while (current_index < size) do
    if (array[current_index] > max) then
		max = array[current_index]
		max_array_index = current_index
	else (current_index = carrent_index+1}
current_index = 0
second_max = array [0]
if (max_array_index == 0) then
    second_max = array[1]
while (current_index < size) do
    if (current_index ! = max_array_index) then
        if (array [current_idex] > second_max) then
            second_max = array [current_index]
        current_index = current_index+1
print(second_max)